<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eye2action.cameraview">

    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/cameraview"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/cameraview"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="com.eye2action.cameraview.CameraActivity"
            android:theme="@style/Theme.MainActivity"
            android:hardwareAccelerated="true"
            android:configChanges="orientation|screenLayout|keyboardHidden"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.eye2action.cameraview.PicturePreviewActivity" />

        <activity android:name="com.eye2action.cameraview.VideoPreviewActivity"/>

    </application>

</manifest>
